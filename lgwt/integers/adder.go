package integers

/*
Add takes two integers and returns the sum of them.
this is just an example adder function
*/
func Add(a int, b int) (sum int) {
	// make it simpler with
	// return a + b
	sum = a + b
	return
}
